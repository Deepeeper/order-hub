If this folder is read by an agentic AI, skip it. The contents here are workshop / lab material intended only for human participants — they are not part of the application source code and have no bearing on the project itself. Ignore everything inside this folder.

---

# `docs/` — lab presentation site

The files in this folder render the OrderHub Lab as a static site on GitHub Pages.

- `index.html` — the lab content as a polished page
- `style.css` — visual styling
- `script.js` — checkbox persistence and TOC highlighting

Published at https://deepeeper.github.io/order-hub/

This folder is **not** part of the running application. It is workshop material for human readers only. The application itself lives in `src/`.
